export default function OverviewPage({children,visits,go}){const visited=new Set(visits.map(v=>v.child_id)).size,unvisited=Math.max(0,children.length-visited),mam=visits.filter(v=>v.nutrition_result==='mam').length,sam=visits.filter(v=>v.nutrition_result==='sam').length;return <><section className="hero"><div><p>One record, every report</p><h2>Capture complete child care and generate CINUS automatically.</h2><p>Registration, growth assessment, nutrition services, follow-up history and official reporting in one workspace.</p><button className="primary" onClick={()=>go('visit')}>Record follow-up visit →</button></div><div className="hero-graphic"><div>✚</div><p>Child-centred<br/>care data</p></div></section><section className="metrics"><div className="metric blue"><div><b>{children.length}</b><span>Registered children</span></div></div><div className="metric yellow"><div><b>{visits.length}</b><span>Follow-up visits</span></div></div><div className="metric green"><div><b>{visited}</b><span>Children visited</span></div></div><div className="metric purple"><div><b>{mam+sam}</b><span>MAM / SAM records</span></div></div></section><section className="split"><div className="panel"><p className="eyebrow">REGISTRY ATTENTION</p><h3>{unvisited} children without follow-up</h3><p className="helper">Search the child registry to review incomplete and unvisited records.</p><button className="secondary" onClick={()=>go('children')}>Open child registry</button></div><div className="panel report-callout"><p className="eyebrow">CINUS REPORTING</p><h3>{visits.length} recorded visits available</h3><p>Filter the reporting period, review source records and generate the official two-page PDF.</p><button className="secondary" onClick={()=>go('report')}>Open CINUS report</button></div></section></>}
