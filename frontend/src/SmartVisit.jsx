import{useEffect,useMemo,useState}from'react'
import WHOHelp from './WHOHelp'
const api=async(p,o)=>{const r=await fetch('/api'+p,o),t=await r.text();let d;try{d=JSON.parse(t)}catch{}if(!r.ok)throw new Error(d?.detail||t);return d}
const F=({label,...p})=><label className="field"><span>{label}</span><input {...p}/></label>
const S=({label,options,...p})=><label className="field"><span>{label}</span><select {...p}>{options.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</select></label>
const blank=()=>({visit_date:new Date().toISOString().slice(0,10),weight:'',height:'',muac:'',edema:false,health_worker:'Clinical Officer',nutrition_result:'normal',referral:'',vitamin_a_dose:'0',deworming_dose:'0',developmental_result:'ndd',development_notes:''})
const ageOf=(a,b)=>{a=new Date(a);b=new Date(b);return(b.getFullYear()-a.getFullYear())*12+b.getMonth()-a.getMonth()-(b.getDate()<a.getDate()?1:0)}
const next=(day,n)=>{const d=new Date(day);d.setDate(d.getDate()+n);return d.toISOString().slice(0,10)}
export default function SmartVisit({children,done}){const linked=sessionStorage.getItem('cinus_follow_child')||new URLSearchParams(location.hash.split('?')[1]||'').get('child'),initial=children.find(c=>String(c.id)===String(linked))||null,[child,setChild]=useState(initial),[q,setQ]=useState(initial?initial.first_name+' '+initial.last_name:''),[f,setF]=useState(blank()),[history,setHistory]=useState([]),[scores,setScores]=useState(null),[editing,setEditing]=useState(null),[msg,setMsg]=useState('Select a child to unlock the form.'),[historyDate,setHistoryDate]=useState(''),[historyPage,setHistoryPage]=useState(1),size=3;const matches=useMemo(()=>q&&!child?children.filter(c=>(c.first_name+' '+c.last_name+' '+c.child_code+' '+(c.phone||'')).toLowerCase().includes(q.toLowerCase())).slice(0,6):[],[q,child,children]),age=child?ageOf(child.date_of_birth,f.visit_date):null,va=age>=6&&age<=59,dw=age>=24&&age<=59,lastVa=history.find(x=>x.vitamin_a_dose),lastDw=history.find(x=>x.deworming_dose),filtered=historyDate?history.filter(x=>x.visit_date===historyDate):history,pages=Math.max(1,Math.ceil(filtered.length/size)),shown=filtered.slice((historyPage-1)*size,historyPage*size);const reload=async c=>setHistory(await api('/children/'+c.id+'/history'));useEffect(()=>{if(!child&&linked){const c=children.find(x=>String(x.id)===String(linked));if(c){sessionStorage.setItem('cinus_follow_child',String(c.id));setChild(c);setQ(c.first_name+' '+c.last_name);setMsg('Child selected. Enter visit measurements.')}}},[children]);useEffect(()=>{if(child){reload(child)}},[child]);useEffect(()=>{if(!child)return;const old=history.find(x=>x.visit_date===f.visit_date);if(old){setEditing(old.id);setF(x=>({...x,visit_date:old.visit_date,weight:old.weight,height:old.height,muac:old.muac,edema:!!old.edema,health_worker:old.health_worker||'',nutrition_result:old.nutrition_result||'normal',referral:old.referral||'',vitamin_a_dose:String(old.vitamin_a_dose||0),deworming_dose:String(old.deworming_dose||0),developmental_result:old.developmental_result||'ndd',development_notes:old.development_notes||''}));setMsg('Existing visit found on this date. The form is now in update mode.')}else{setEditing(null)}},[child,f.visit_date,history]);useEffect(()=>{if(!child||!f.weight||!f.height)return setScores(null);const t=setTimeout(()=>api('/growth-assessment?child_id='+child.id+'&visit_date='+f.visit_date+'&weight='+f.weight+'&height='+f.height).then(x=>{setScores(x);setMsg('WHO growth scores calculated successfully.')}).catch(e=>{setScores(null);setMsg(e.message)}),250);return()=>clearTimeout(t)},[child,f.visit_date,f.weight,f.height]);const u=e=>setF({...f,[e.target.name]:e.target.type==='checkbox'?e.target.checked:e.target.value,...(e.target.name==='edema'&&e.target.checked?{nutrition_result:'sam'}:{})}),save=async e=>{e.preventDefault();try{const body={...f,child_id:child.id,weight:+f.weight,height:+f.height,muac:+f.muac,vitamin_a_dose:+f.vitamin_a_dose,deworming_dose:+f.deworming_dose};await api(editing?'/visits/'+editing:'/visits',{method:editing?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});await reload(child);setMsg(editing?'Visit updated successfully.':'New visit saved successfully.');done(editing?'Visit updated.':'Follow-up saved.')}catch(x){setMsg('Cannot save: '+x.message)}};return <><div className="followup-help-row"><WHOHelp/></div><div className="followup-layout"><section className="panel form-card"><p className="eyebrow">SEARCH CHILD</p><F label="Name, CIN code or phone" value={q} onChange={e=>{setQ(e.target.value);sessionStorage.removeItem('cinus_follow_child');setChild(null)}}/>{matches.length>0&&<div className="search-results">{matches.map(c=><button type="button" key={c.id} onClick={()=>{sessionStorage.setItem('cinus_follow_child',String(c.id));setChild(c);setQ(c.first_name+' '+c.last_name+' · '+c.child_code);setMsg('Child selected. Enter visit measurements.')}}><b>{c.first_name} {c.last_name}</b><small>{c.child_code} · DOB {c.date_of_birth}</small></button>)}</div>}{!child&&<div className="select-alert"><b>Select a child to begin</b><span>The complete follow-up form is locked until a registered child is selected.</span><div className="quick-child-list">{children.slice(0,8).map(c=><button type="button" key={c.id} onClick={()=>{sessionStorage.setItem("cinus_follow_child",String(c.id));setChild(c);setQ(c.first_name+" "+c.last_name+" · "+c.child_code);setMsg("Child selected. Enter visit measurements.")}}><strong>{c.first_name} {c.last_name}</strong><small>{c.child_code} · {c.woreda||"Registered child"}</small></button>)}</div></div>}{child&&<div className={'selected-child '+(editing?'editing-visit':'')}><b>{child.first_name} {child.last_name}</b><span>{editing?'Editing visit '+f.visit_date:child.child_code+' · '+age+' months'}</span></div>}<form onSubmit={save}><fieldset disabled={!child}><div className="form-grid cols-3"><F label="Visit date" type="date" name="visit_date" value={f.visit_date} onChange={u}/><F label="Health worker" name="health_worker" value={f.health_worker} onChange={u}/><F label="Weight (kg)" type="number" step=".1" name="weight" value={f.weight} onChange={u}/><F label="Length / height (cm)" type="number" step=".1" name="height" value={f.height} onChange={u}/><F label="MUAC (cm)" type="number" step=".1" name="muac" value={f.muac} onChange={u}/></div><div className="section-title">Automatic WHO assessment</div><div className="form-grid cols-3"><F label="WAZ" value={scores?.waz??''} readOnly/><F label="HAZ" value={scores?.haz??''} readOnly/><F label="WHZ" value={scores?.whz??''} readOnly/></div><div className="checks"><label><input type="checkbox" name="edema" checked={f.edema} onChange={u}/><span>Bilateral pitting oedema (SAM)</span></label></div><div className="form-grid cols-3"><S label="Nutrition" name="nutrition_result" value={f.edema?'sam':f.nutrition_result} disabled={f.edema} onChange={u} options={[['normal','Normal'],['mam','MAM'],['sam','SAM']]}/><S label="Vitamin A" name="vitamin_a_dose" value={f.vitamin_a_dose} disabled={!va} onChange={u} options={[[0,'No dose'],[1,'Dose 1'],[2,'Dose 2']]}/><S label="Deworming" name="deworming_dose" value={f.deworming_dose} disabled={!dw} onChange={u} options={[[0,'No dose'],[1,'Dose 1'],[2,'Dose 2']]}/><S label="Development" name="developmental_result" value={f.developmental_result} onChange={u} options={[['ndd','NDD'],['sdd','SDD'],['cdd','CDD']]}/><F label="Referral / action" name="referral" value={f.referral} onChange={u}/><F label="Development notes" name="development_notes" value={f.development_notes} onChange={u}/></div><p className={"helper "+((msg.startsWith("Cannot")||msg.includes("must be")||msg.includes("Check measurements"))?"warning":"info")}>{msg}</p><button className="primary">{editing?'Update visit for '+f.visit_date:'Save new follow-up'}</button></fieldset></form></section><aside className="panel child-insight"><p className="eyebrow">HISTORY & RECOMMENDATIONS</p>{child&&<><div className="recommendations"><p><b>Vitamin A:</b> {!va?'Not age eligible':lastVa?'Last '+lastVa.visit_date+' · next '+next(lastVa.visit_date,120):'No dose recorded · eligible now'}</p><p><b>Deworming:</b> {!dw?'Available from 24 months':lastDw?'Last '+lastDw.visit_date+' · next '+next(lastDw.visit_date,365):'No dose recorded · eligible now'}</p></div><F label="Filter history by visit date" type="date" value={historyDate} onChange={e=>{setHistoryDate(e.target.value);setHistoryPage(1)}}/>{shown.map(v=><article className="history-card detailed" key={v.id} onClick={()=>setF(x=>({...x,visit_date:v.visit_date}))}><b>{v.visit_date}</b><span>Weight {v.weight} kg · Height {v.height} cm · MUAC {v.muac} cm</span><span>WAZ {v.waz??'—'} · HAZ {v.haz??'—'} · WHZ {v.whz??'—'}</span><small>{(v.nutrition_result||'—').toUpperCase()} · Development {(v.developmental_result||'—').toUpperCase()}</small><small>Vitamin A {v.vitamin_a_dose||'—'} · Deworming {v.deworming_dose||'—'} · {v.health_worker||'No worker'}</small></article>)}<div className="pagination compact-pages"><span>{historyPage}/{pages}</span><div><button disabled={historyPage===1} onClick={()=>setHistoryPage(historyPage-1)}>←</button><button disabled={historyPage===pages} onClick={()=>setHistoryPage(historyPage+1)}>→</button></div></div></>}</aside></div></>}
